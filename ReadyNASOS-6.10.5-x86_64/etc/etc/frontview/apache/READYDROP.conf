<Location ~ "/cgi-bin/(delete|download|makedir|status|upload|fastupload).cgi">
        Order Deny,Allow
        Deny from All
        Include /etc/frontview/apache/Auth.conf
        AuthName "ReadySync Client"
        require valid-user
        Allow from 5.0.0.0/8
        Satisfy Any
</Location>
